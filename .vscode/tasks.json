{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build and run engine",
            "type": "shell",
            "command": "cd build/ && make && ./engine",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}