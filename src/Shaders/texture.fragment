#version 330 core

in vec2 TexCoords;

out vec4 color;

uniform sampler2D screenTexture;

void main() {
	vec4 cTex = texture(screenTexture, TexCoords).rgba;
	color = vec4(cTex.r, cTex.g, cTex.b, cTex.a);
}